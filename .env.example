<<<<<<< HEAD
# BoerenKompas Environment Variables
# Copy this file to .env.local and fill in your real values
# 
# Get your Supabase credentials from:
# https://supabase.com/dashboard/project/_/settings/api

# =============================================================================
# REQUIRED: Supabase Public Config
# =============================================================================

# Your Supabase project URL (NOT the placeholder below!)
# Format: https://YOUR_PROJECT_REF.supabase.co
# Example: https://abcd1234efgh5678.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co

# Your Supabase anon/public key (a long JWT string, ~200 chars)
# This is safe to expose in the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE

# =============================================================================
# OPTIONAL: Feature Flags
# =============================================================================

# Enable demo login button (set to 'true' only for development/demo)
NEXT_PUBLIC_ENABLE_DEMO_LOGIN=false

# =============================================================================
# SERVER-ONLY (Never expose these in NEXT_PUBLIC_ vars!)
# =============================================================================

# Supabase service role key - KEEP SECRET, server-side only
# Only needed for admin operations
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
=======
# ═══════════════════════════════════════════════════════════════════════════════
# BoerenKompas Environment Variables
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env.local and fill in your credentials:
#   cp .env.example .env.local
#
# Run `npm run doctor:env` to validate your configuration.
# ═══════════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────────
# REQUIRED: Supabase Credentials
# Get from: Supabase Dashboard → Settings → API
# ───────────────────────────────────────────────────────────────────────────────

# Supabase Project URL (public, safe for client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key (public, RLS enforced)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key

# Supabase Service Role Key (SECRET - server only, bypasses RLS)
# Required for: public export sharing, document downloads, dev seeding
# ⚠️  NEVER expose to client code or commit to git
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key

# ───────────────────────────────────────────────────────────────────────────────
# REQUIRED: Application URL
# Used for: Stripe redirects, email links, share URLs
# ───────────────────────────────────────────────────────────────────────────────

# Public app URL (must match your Supabase Auth redirect URLs)
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Server-side app URL fallback (optional, uses NEXT_PUBLIC_APP_URL if not set)
# APP_URL=http://localhost:3001

# ───────────────────────────────────────────────────────────────────────────────
# REQUIRED FOR BILLING: Stripe
# Get from: Stripe Dashboard → Developers → API keys / Webhooks / Products
# Skip these if not using paid plans
# ───────────────────────────────────────────────────────────────────────────────

# Stripe Secret Key (server only)
# Test mode: sk_test_... / Live mode: sk_live_...
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Stripe Webhook Signing Secret
# Get from: Stripe Dashboard → Developers → Webhooks → Signing secret
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret

# Stripe Price IDs for Pro plan
# Get from: Stripe Dashboard → Products → Select product → Prices
STRIPE_PRICE_PRO_MONTHLY=price_your_monthly_price_id
STRIPE_PRICE_PRO_ANNUAL=price_your_annual_price_id

# ───────────────────────────────────────────────────────────────────────────────
# OPTIONAL: Development Tools
# ───────────────────────────────────────────────────────────────────────────────

# Enable /api/dev/seed endpoint for test data
# Usage: POST /api/dev/seed with header X-DEV-SEED-SECRET: <this value>
# DEV_SEED_SECRET=your-random-secret-here

# Show demo login button on /login page
# NEXT_PUBLIC_ENABLE_DEMO_LOGIN=true

# Enable plan preview switcher in development (test Pro features without billing)
# NEXT_PUBLIC_ENABLE_PLAN_PREVIEW=true
>>>>>>> b0318de (chore: sync updates)
